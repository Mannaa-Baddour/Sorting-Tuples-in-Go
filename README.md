# Sorting - Playground Learning Project

## Sort

To run sort cmd directly from code

```bash
go run cmd/sort/sort.go
```

To build and run

```bash
make build-sort
./bin/sort
```

To build all
```
make build
```

To clean build
```
make clean
```